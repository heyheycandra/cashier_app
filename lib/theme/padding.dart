import 'package:flutter/material.dart';

const cashierOutterMargin = EdgeInsets.only(left: 17, right: 17, top: 16);

const cashierMargin = EdgeInsets.only(left: 17, right: 17, top: 16);

const cashierMarginHorizontal = EdgeInsets.only(left: 17, right: 17);

const cashierMarginBtn = EdgeInsets.only(top: 12, bottom: 12);

const cashierDashboardPadding = EdgeInsets.only(top: 16, bottom: 16);

const cashierOutterPadding = EdgeInsets.only(top: 16, right: 24, left: 24);

const cashierInnerPadding = EdgeInsets.only(top: 16, right: 12, left: 12, bottom: 16);

const cashierSearchFieldPadding = EdgeInsets.only(top: 16, right: 17, left: 17);

const cashierParagraph = EdgeInsets.only(left: 8);

const cashierBoxedParagraph = EdgeInsets.only(left: 8, top: 16, bottom: 16);

const cashierDialogPadding = EdgeInsets.only(top: 30, right: 24, left: 24, bottom: 20);
